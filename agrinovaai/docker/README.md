# Docker Notes

The root `docker-compose.yml` builds the application services and uses `nginx/nginx.conf` as the public reverse proxy.

This directory is reserved for future deployment overlays such as TLS certificates, Kubernetes manifests, or cloud-specific compose overrides.
