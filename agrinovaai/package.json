{
  "name": "agrinova-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AgriNova AI full-stack agritech intelligence platform",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"python ai-service/app/main.py\"",
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend",
    "zip": "powershell -NoProfile -ExecutionPolicy Bypass -File docker/create-zip.ps1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
